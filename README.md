# Svelte development environment base on Docker container

Use this command to run development project.

```
docker compose up
```

You can open this url: [http://localhost:5173/](http://localhost:5173/) to preview page.
